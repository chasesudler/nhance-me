import AppShell from '@/components/AppShell';
import Link from 'next/link';
import { Images, Sparkles, Sofa, Clock } from 'lucide-react';
export default function Dashboard(){return <AppShell><div className="topbar"><div><h1>Dashboard</h1><p className="small">Manage batch enhancements, staging jobs, presets, and downloads.</p></div><Link className="btn btn-primary" href="/process">New batch</Link></div><div className="cards"><div className="card metric"><Images/><span>Batch limit</span><b>50</b></div><div className="card metric"><Sparkles/><span>Enhancement presets</span><b>6</b></div><div className="card metric"><Sofa/><span>Staging styles</span><b>5</b></div><div className="card metric"><Clock/><span>Queue mode</span><b>Ready</b></div></div><section className="section"><div className="card"><h2>Production blueprint</h2><p className="lead">This MVP uses instant browser previews and local downloads. For a paid production app, connect the included API routes to a real image model pipeline, queue jobs, store originals securely, then serve signed download links from blob storage.</p><div className="grid-3"><div className="feature"><h3>Faithful enhancement</h3><p>Use deterministic corrections first: exposure, white balance, lens/perspective, HDR merge, denoise and sharpness. Add generative features only where user explicitly requests them.</p></div><div className="feature"><h3>50-image continuity</h3><p>Apply one preset across a listing batch and preserve style metadata so interiors/exteriors feel consistent from first photo to last.</p></div><div className="feature"><h3>Virtual staging</h3><p>Route staging requests separately from faithful enhancements to protect trust and avoid hallucinated property conditions.</p></div></div></div></section></AppShell>}
